---
Source:
  - https://youtube.com/watch?v=xR82Jx4pqIc
Reviewed: false
---
index.js
```javascript
// Math =  an intrinsic object that provides basic mathematics functionality and constants

let x = 3.14;
let y = 5;
let z = 9;
let maximum;
let minimum;

//x = Math.round(x); //rounds number to closest integer
//x = Math.floor(x); //rounds number down
//x = Math.ceil(x); //rounds rounded up
//x = Math.pow(x, 2); //power of ?
//x = Math.sqrt(x); //finds square root
//x = Math.abs(x); //absolute value
//maximum = Math.max(x, y, z); //finds maximum of numbers
//minimum = Math.min(x, y, z); //finds minimum of numbers
//x = Math.PI; //A built-in constant. Has more digits within

console.log(x);
```

index.html
```HTML
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <label id="myLabel">Enter your name:</label><br>
    <input type="text" id="myText"><br>
    <button type="button" id="myButton">submit</button>
    <script src="index.js"></script>

</body>
</html>
```

Output:
`3.14`