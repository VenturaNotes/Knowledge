---
aliases:
  - operator
  - operators
---
## Synthesis
- / is used for [[true division (Python)|true division]]
## Source [^1]

```python
# Multiplying two operands
print(10 * 5) #Output: 50

# Dividing two operands
print(10/2) #Output: 5.0
```

[[membership operator (python)|membership operator]]
```python
fruits = ["apple", "banana"]
if "apple" in fruits:
  print("Yes, apple is a fruit!")

# The membership operator here is "in"
```

[[comparison operator (python)|comparison operator]]
```python
if 5 != 10:
	print("5 and 10 is not equal)

# The comparison operator here is "!="
```

[[logical operator (python)|logical operator]]
```python
if 5 == 10 or 4 == 4:
	print("At least one of the statements is true")

# logical operator is "or"
```
## References

[^1]: https://www.w3schools.com/python/exercise.asp?filename=exercise_operators1