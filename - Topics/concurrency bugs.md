## Synthesis
- An undesired outcome when two programs are run at the same time
	- #question Are you sure it's "programs" or would it be threads in this instance?
## Source [^1]
- A concurrency bug is an undesired outcome that arises if two programs are run at the same time that does not show up if the two programs are run sequentially, one after the other
	- This kind of problem can show up when updating databases or data structures. 
## References

[^1]: https://www.quora.com/What-are-concurrency-bugs