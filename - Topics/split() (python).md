---
aliases:
  - split()
---
## Synthesis
- The default separator is whitespace
## Source [^1]
- The `split()` method splits a string into a list
- You can specify the separator. The default separator is any whitespace. 
	- #question Is there a difference between separator and delimiter? 
- When `maxsplit` is specified, the list will contain the specified number of elements plus one
	- #question What is maxsplit?
## References

[^1]: https://www.w3schools.com/python/ref_string_split.asp