## Synthesis
- 
## Source [^1]
- Let $K$ denote the splitting field of a polynomial $f(x)$ over $\mathbb{Q}$ and let $G$ denote the Galois group of $K: \mathbb{Q}$. Then the degree of $K: \mathbb{Q}$ equals the order of $G$. Further for every subgroup $H$ of $G$ we can define its fixed field$$H^\dagger = \{k \in K | g(k) = k \text{ for all } g \in H\},$$which is a subfield of $K$. And for every subfield $F$ of $K$,$$F^* = \text{Gal}(K:F) = \{g \in G | g(f) = f \text{ for all } f \in F\}$$is a subgroup of $G$. Then $*$ and $\dagger$ are inverses of one another. Further $N$ is a normal subgroup of $G$ if and only if $N^\dagger$ is the splitting field of some polynomial. (With further technical additions the correspondence can be extended to base fields other than $\mathbb{Q}$.)
## References

[^1]: [[(Home Page) The Concise Oxford Dictionary of Mathematics 6th Edition by Oxford Reference]]